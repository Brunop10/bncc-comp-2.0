# Copie este arquivo para `.env` e ajuste os valores.

# URL do Web App do Google Apps Script (usado como backend)
VITE_API_APP_SHEET_URL=sua_url

# Chave pública VAPID para Push Notifications (mostra prompt e assina no cliente)
VITE_PUSH_PUBLIC_KEY=sua_chave

# Chave da API do Gemini 
VITE_GEMINI_API_KEY=sua_chave

# Google Sheets: planilha que recebe as avaliações
GOOGLE_SPREADSHEET_ID=id_da_sua_planilha

# Conta de serviço do Google (e-mail)
GOOGLE_SERVICE_ACCOUNT_EMAIL=seu_email_de_servico

# Chave privada da conta de serviço (com quebras de linha escapadas)
# Use \n para representar quebras de linha.
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=sua_chave

# Opcional: protege o endpoint de broadcast com Authorization: Bearer <segredo>
BROADCAST_SECRET=sua_chave
